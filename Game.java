import java.util.List;

abstract class Game {
    protected Hand hand;
    protected Deck deck;
    protected int numberOfCards;
    /**
     * Returns the List of cards in hand
     *
     * @return List<Card>
     */
    //abstract void evaluate(List<Card> hand);
}